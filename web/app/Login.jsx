define(['react'], function(React) {
    var Login = React.createClass({
        render: function() {
            return (
                <p>Login</p>
            );
        }
    });

    return Login;
})
